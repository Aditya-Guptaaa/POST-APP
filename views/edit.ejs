 <!-- <!DOCTYPE html>
 <html lang="en">
 <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
     <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
 </head>
 <body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
        <div class="flex w-full justify-end">
            <a href="/logout" class="bg-red-500 rounded-md px-3 py-2 mb-5 inline-block">Logout</a>
        </div> 
        
       
        <h5 class="mb-5">Edit Your post</h5>

        <form action="/update/<%=post._id%>" method="post">
            <textarea name="content" placeholder="Whaht's on your mind?" class="w-1/3 p-3 block outline-none resize-none bg-transparent border-2 border-zinc-800 rounded-md"><%= post.content%></textarea>
            <input type="submit" class= " p-3 block bg-yellow-500 text-black rounded-md mt-2 w-40" value="Update post">
        </form>

    </div>
 </body>
 </html>

  -->

  <!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Edit Post</title>
  <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body class="bg-zinc-900 text-white min-h-screen flex items-center justify-center px-4">

  <div class="w-full max-w-xl bg-zinc-800 p-8 rounded-2xl shadow-xl">
    <div class="flex justify-between items-center mb-6">
      <h2 class="text-2xl font-semibold text-white">Edit Your Post</h2>
      <a href="/logout" class="bg-red-500 hover:bg-red-600 transition px-4 py-2 rounded-md font-medium text-white">
        Logout
      </a>
    </div>

    <form action="/update/<%=post._id%>" method="post">
      <label class="block mb-2 text-zinc-300 text-sm font-medium">Your Thoughts</label>
      <textarea 
        name="content" 
        placeholder="What's on your mind?" 
        class="w-full h-40 p-4 bg-zinc-900 border border-zinc-700 rounded-md resize-none focus:outline-none focus:ring-2 focus:ring-yellow-500 transition"
      ><%= post.content %></textarea>

      <input 
        type="submit" 
        value="Update Post" 
        class="mt-4 w-full bg-yellow-500 hover:bg-yellow-400 text-black font-semibold py-3 rounded-md transition cursor-pointer"
      />
    </form>
  </div>

</body>
</html>
